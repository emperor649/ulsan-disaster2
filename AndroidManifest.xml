<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:name=".DisasterAlertApp"
        android:theme="@style/Theme.DisasterAlert"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.RegionSettingsActivity"
            android:exported="false"
            android:label="@string/region_settings" />

        <activity
            android:name=".ui.EventsActivity"
            android:exported="false"
            android:label="@string/events" />

        <activity
            android:name=".ui.ControlSitesActivity"
            android:exported="false"
            android:label="@string/control_sites" />

        <activity
            android:name=".ui.TyphoonActivity"
            android:exported="false"
            android:label="@string/typhoon_status" />

        <activity
            android:name=".ui.CalibrationActivity"
            android:exported="false"
            android:label="@string/calibration" />

        <activity
            android:name=".ui.ApiDiagnosticsActivity"
            android:exported="false"
            android:label="@string/api_diagnostics" />

        <activity
            android:name=".ui.TideActivity"
            android:exported="false"
            android:label="@string/tide_status" />

        <activity
            android:name=".ui.RiverLevelActivity"
            android:exported="false"
            android:label="@string/river_level" />

        <activity
            android:name=".ui.HistoryAnalysisActivity"
            android:exported="false"
            android:label="@string/history_analysis" />

        <activity
            android:name=".ui.AlertDetailActivity"
            android:exported="false"
            android:label="@string/alert_detail" />

        <!-- 서버(FCM)에서 재난 위기경보 즉시 push
             Firebase 사용 시 아래 블록 주석 해제
        <service
            android:name=".notification.DisasterFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        -->

        <receiver
            android:name=".worker.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Firebase 사용 시 주석 해제
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/channel_id_critical" />
        -->

    </application>

</manifest>
