# 빌드 문제 해결

제가 코드를 정적 점검했습니다. 아래는 이상 없었습니다.

| 점검 항목 | 결과 |
|---|---|
| 괄호 균형 (56개 파일) | 이상 없음 |
| ViewBinding ↔ 레이아웃 ID | 이상 없음 |
| 문자열·drawable 리소스 | 이상 없음 |
| Manifest 액티비티 등록 | 이상 없음 |
| 중복 선언 | 없음 |
| suspend 함수 호출 컨텍스트 | 이상 없음 |
| 미사용 import | 정리 완료 |

**다만 정적 점검은 컴파일러를 대신하지 못합니다.**
타입 불일치, null 안정성, 제네릭 추론 같은 건 실제로 컴파일해야 나옵니다.
아래는 그때 만날 가능성이 높은 것들입니다.

---

## 1. Gradle Wrapper 없음 — 가장 먼저 만날 문제

`gradle-wrapper.jar`와 `gradlew` 스크립트가 zip에 없습니다.
바이너리 파일이라 제가 만들어 넣을 수 없었습니다.

**해결: Android Studio가 자동으로 만들어 줍니다.**

프로젝트를 열면 이런 창이 뜹니다.
```
Gradle wrapper not found. Use Gradle from: [Gradle wrapper] [Specified location]
```
→ **Gradle wrapper** 선택 후 OK

또는 상단 메뉴에서:
`File` → `Sync Project with Gradle Files`

이래도 안 되면 터미널에서 (Android Studio 하단 Terminal 탭):
```
gradle wrapper --gradle-version 8.5
```

---

## 2. JDK 버전

AGP 8.2.2는 **JDK 17**이 필요합니다.

`File` → `Settings` → `Build, Execution, Deployment` → `Build Tools` → `Gradle`
→ **Gradle JDK**를 `jbr-17` 또는 `JetBrains Runtime 17`로 설정

JDK 11 이하면 이런 오류가 납니다.
```
Android Gradle plugin requires Java 17 to run. You are currently using Java 11.
```

---

## 3. SDK 미설치

`compileSdk 34`를 씁니다. 없으면:
```
Failed to find target with hash string 'android-34'
```

`Tools` → `SDK Manager` → `Android 14.0 (API 34)` 체크 → Apply

---

## 4. 예상되는 Kotlin 컴파일 오류

정적 점검으로는 못 잡는 것들입니다. 나오면 아래를 참고하세요.

### `Type mismatch: inferred type is Double but Int was expected`
점수 계산에서 Double/Int가 섞입니다.
`CompositeRiskCalculator`의 `score` 변수가 Double인데 Int를 더하는 지점일 가능성이 큽니다.
→ `.toDouble()` 또는 `.toInt()` 추가

### `Smart cast to 'X' is impossible`
nullable 프로퍼티를 검사 후 바로 쓰는 경우입니다.
→ `?.let { }` 로 감싸거나 지역 변수에 담기

### `Unresolved reference: awsApi` 등
`ApiClient`에 프로퍼티는 추가했지만 import가 빠졌을 수 있습니다.
→ 해당 파일에서 Alt+Enter로 자동 import

### `@Streaming` 관련 경고
`AwsApiService`, `TyphoonApiService`가 `okhttp3.ResponseBody`를 반환합니다.
Retrofit에서 정상 동작하지만 경고가 뜰 수 있습니다. 무시해도 됩니다.

---

## 5. 오류를 만나면

**에러 메시지 전문을 그대로 주세요.** 파일명과 줄 번호가 중요합니다.

Android Studio 하단 `Build` 탭 → 빨간 줄 우클릭 → `Copy`

한 번에 여러 개가 나와도 괜찮습니다. 대부분 같은 원인에서 파생됩니다.

---

## 6. 빌드 성공 후 첫 확인

APK가 올라가면 앱을 켜고 **`임계값 검증`** 버튼을 누르세요.
API 키 없이 동작하므로, 이게 나오면 계산 로직이 살아 있다는 뜻입니다.

그 다음 **`통제지점`** 버튼 → 슬라이더를 올려보세요.
216개소가 기준 도달 순으로 뜨면 데이터도 정상입니다.

마지막으로 **`API 진단`** — 키를 넣었으면 여기서 연결 상태가 나옵니다.
